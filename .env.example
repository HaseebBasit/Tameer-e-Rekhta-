# Ta'meer-e-Rekhta Environment Variables
# Copy this file to .env.local to customize local or production settings

# Recipient email where volunteer applications will be delivered
VOLUNTEER_RECIPIENT_EMAIL=haseebbasit2717@gmail.com

# (Optional) SMTP Configuration for direct Nodemailer dispatch (e.g., Gmail App Password)
# If left blank, applications will automatically be sent using the built-in direct server delivery.
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=465
# SMTP_SECURE=true
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-google-app-password
